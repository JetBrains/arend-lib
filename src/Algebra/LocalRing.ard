\import Algebra.Group
\import Algebra.Monoid
\import Algebra.Ring
\import Logic
\import Relation.Apartness
\open CMonoid(isInv)

\class LocalRing \extends CRing {
  | nonTrivial : Not (addId = id)
  | locality (x y : E) : x + y = id -> isInv x || isInv y
}